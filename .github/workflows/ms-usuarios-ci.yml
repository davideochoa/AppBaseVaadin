name: ms-usuarios CI

on:
  push:
    branches: ["**"]
    paths:
      - "ms-usuarios/**"
      - ".github/workflows/ms-usuarios-ci.yml"
  pull_request:
    paths:
      - "ms-usuarios/**"
      - ".github/workflows/ms-usuarios-ci.yml"

defaults:
  run:
    working-directory: ms-usuarios

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Build and run tests (Testcontainers via Docker on the runner)
        run: mvn --batch-mode --update-snapshots clean verify

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ms-usuarios-surefire-reports
          path: ms-usuarios/target/surefire-reports
          if-no-files-found: ignore
